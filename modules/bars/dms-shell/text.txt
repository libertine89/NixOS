in niri module
ctx = {
  inherit pkgs lib bar wallpaper;
  inherit ide-launcher toggle-dropdown;
}
// lib.optionalAttrs (bar == "dms") {
  inherit dmsWrapper;
};

in autostar.nix
lib.optionalString (bar == "dms") ''
  spawn-at-startup "${lib.getExe dmsWrapper}"
''
